#include <stdio.h>
int main(){
	char buff[100];
	printf("Enter the string: ");
	fgets(buff,sizeof(buff),stdin);
	puts(buff);
	return 0;
}
//terrenary operator
//display character
//display string
//display a group of strings
